[package]
name = "api-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
reqwest = { version = "0.11", features = ["json"] }
tokio = { workspace = true, features = ["full"] }
